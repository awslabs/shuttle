# Shuttle support for `std::sync`

This crate contains the implementation that enables testing of applications that use `std::sync` with Shuttle. It should not be depended on directly, depend on `shuttle-sync` instead.
